TODO:
- HW Active Balancer 1.0.0.0:
    - Routing PCB

- HW master changes for v1.5.0.0:
    - safe Bat fault only on save relay
    - sic dirver seperately for top/bot
    - precharge relay with 100Ohm 100W resistor in parallel to main switch

- SW Future improvements
    - add ota at startup
    - add mqtt support
    - add crc check to BMSnowProtocol's
    - Add CAN support
    - Add sic driver (maybe relay driver?)
    - Protection manager:
        - Trigger save relay if slave a is lost. (see BMSnow::_connection_supervisor_task)
        - Trigger save relay if current over +/- max_current + X (config)
        - Trigger save relay if temperatures over 60 C° (config)
        - Trigger save relay if pack voltage is over/under protecion minimum (config)
        - Trigger save relay if string voltage is over/under protecion minimum (config)
        - Trigger save relay if cell voltage is over/under protecion threshold (config)
    - Power manager


