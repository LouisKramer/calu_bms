TODO:
- HW Active Balancer 1.0.0.0:
    - Routing PCB
- Future improvements
    - add ota at startup
    - add mqtt support
    - add crc check to BMSnowProtocol's
    - Add CAN support
    - Add sic driver (maybe relay driver?)
    - Protection:
        - Trigger save relay if slave is lost. (see BMSnow::_connection_supervisor_task)
        - Trigger save relay if current over 25A + 2A
        - Trigger save relay if temperatures over 40 C°

